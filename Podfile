# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GrayscaleMode' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GrayscaleMode
  pod 'LaunchAtLogin'
  pod 'Defaults'

  target 'GrayscaleModeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GrayscaleModeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
